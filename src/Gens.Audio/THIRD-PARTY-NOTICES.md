# Gens.Audio third-party notice

Ogg Vorbis decoding is provided by [NVorbis](https://github.com/NVorbis/NVorbis)
(NuGet package `NVorbis`, referenced by `Gens.Audio.csproj`), a pure-managed,
`netstandard2.0`-compatible decoder distributed under the MIT license. It
carries no native/platform-specific binary, so it needs no additional
packaging step for self-contained publishes.
